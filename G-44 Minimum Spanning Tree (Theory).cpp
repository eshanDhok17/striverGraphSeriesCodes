/*
  Definition of MST :
      A tree in which we have N nodes and N-1 Edges and all nodes are
      connected to each other directly or indirectly is a spanning tree.
      The Spanning tree where sum of all edgeweights in that ST is minimum.
      There can only be 1 MST and multiple ST.
*/
